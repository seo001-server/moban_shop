export const MAX_TABS = 10
export const HOME_TAB_KEY = '/'
export const TAB_STORAGE_KEY = 'moban_admin_tabs_v1'

/** List pages where query params are filters — one tab per pathname. */
export const LIST_TAB_PATHS = new Set(['/users', '/templates', '/business', '/orders', '/audit-logs'])
